fileGetter.py — Line-by-Line Explanation
==========================================

Lines 1–7: Module Docstring
-----------------------------
A multi-line docstring describing the file's purpose: it's a FastAPI server
that provides a POST endpoint for uploading dance videos, runs the comparison
pipeline, and returns feedback results. It also uses Server-Sent Events (SSE)
for real-time progress updates.


Lines 8–18: Imports
---------------------
- asyncio: Async utilities — Queue for SSE messages, wait_for with timeout.
- json: JSON serialization — used in save_results to write results.json.
- os: File system operations — joining paths, checking existence, creating directories.
- shutil: High-level file operations — copyfileobj copies uploaded file data to disk.
- sys: System-level — modifying sys.path so sibling packages can be imported.
- datetime (from datetime): Generates timestamps for session IDs.
- FastAPI (from fastapi): The web framework class — creates the app.
- File (from fastapi): Marker that declares a parameter as a required file upload.
- HTTPException (from fastapi): Used to raise HTTP error responses (e.g. 500).
- UploadFile (from fastapi): Type representing an uploaded file, with .file and .filename attributes.
- CORSMiddleware (from fastapi.middleware.cors): Adds Cross-Origin Resource Sharing headers.
- StreamingResponse (from fastapi.responses): Wraps an async generator for SSE streaming.
- uvicorn: ASGI server to run the FastAPI app.


Lines 20–23: Path Setup
--------------------------
- CURRENT_DIR: The absolute path of the directory this file lives in (controller/).
- ROOT_DIR: The parent directory (project root).
- Lines 22–23: If ROOT_DIR is not already in Python's module search path, insert it
  at position 0 so imports like "from model.xxx import yyy" work.


Line 25: Project Import
-------------------------
- process_videos: The main pipeline function from model/video_processor.py that
  processes and compares two dance videos. Must come AFTER the sys.path setup.


Lines 27–35: App Creation and CORS
-------------------------------------
- Line 27: Creates the FastAPI application instance (no lifespan hook, unlike feedbackSender).
- Lines 29–35: Adds CORSMiddleware allowing requests from any origin (*), all HTTP methods
  and headers, with credentials (cookies/auth) permitted.


Lines 37–41: Directory Constants and Setup
--------------------------------------------
- UPLOAD_FOLDER: Path to root/uploaded_videos/ — where uploaded video files are saved.
- DATA_DIR: Path to root/data/ — where session result directories are stored.
- Lines 40–41: Creates the upload directory at import time if it doesn't exist.


Line 43: SSE Infrastructure
------------------------------
- status_queue: An asyncio.Queue that holds status messages. The pipeline writes messages
  into this queue, and the SSE event_generator reads from it to send updates to the frontend.


Lines 46–91: FileGetter Class
-------------------------------
A class that groups file handling and result persistence logic as static methods.
It contains no instance state — all methods are @staticmethod.

  save_results(session_id: str, results: dict) -> None  (lines 49–78)
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  Persists comparison results to disk as JSON.
  - Line 59: Lazy-imports numpy (only when actually saving, to avoid startup cost).
  - Lines 61–62: Builds the session directory path (data/<session_id>/) and creates it if needed.
  - Lines 64–74: Iterates over the results dict and builds a JSON-serializable copy:
      - numpy arrays -> converted to Python lists with .tolist()
      - nested dicts -> any numpy arrays inside are also converted
      - everything else -> kept as-is
  - Lines 76–78: Writes the serializable dict to results.json with 2-space indentation.

  event_generator()  (lines 80–91)
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  An async generator that yields Server-Sent Events (SSE) to the client.
  - Line 86: Infinite loop — keeps the connection alive indefinitely.
  - Line 88: Tries to get a message from status_queue with a 1-second timeout
    using asyncio.wait_for().
  - Line 89: If a message arrives, yields it in SSE format: "data: <message>\n\n".
  - Lines 90–91: If the timeout expires (no messages), yields a comment line
    (": keep-alive\n\n") to prevent the browser from closing the connection.


Lines 97–100: GET /events Endpoint
-------------------------------------
- Returns a StreamingResponse wrapping FileGetter.event_generator().
- media_type="text/event-stream" tells the browser this is an SSE stream.
- The frontend connects to this endpoint using EventSource for real-time pipeline updates.


Lines 103–152: POST /dance_videos Endpoint
--------------------------------------------
The main endpoint. Accepts two required file uploads: teacher and student.
File(...) means both are required — the request fails without them.

- Lines 118–119: Build file paths using the original filenames inside UPLOAD_FOLDER.
- Lines 121–122: Save the teacher video — shutil.copyfileobj copies the file-like object
  from the upload to a file on disk opened in write-binary mode.
- Lines 124–125: Same for the student video.
- Lines 127–128: Define sse_status_handler, a closure that puts messages into
  status_queue. This is passed to process_videos so the pipeline can send
  real-time progress updates to the frontend.
- Line 130: Generate a session ID from the current timestamp (format: YYYYMMDD_HHMMSS).
- Line 131: Build the output directory path: data/<session_id>/.
- Line 133: Call process_videos() with both video paths, the output directory, and
  the SSE handler. This is awaited because it's an async function.
- Lines 135–147: If processing succeeded (results is not None):
    - Line 136: Calls FileGetter.save_results() to persist the results JSON to disk.
    - Line 137: Sends "Processing complete." via SSE.
    - Lines 139–147: Returns a dict with success message, session_id, and the main
      scores (overall, skeleton, trajectory, mask) plus feedback messages.
- Lines 148–149: If results is None, raises a 500 HTTP error.
- Lines 151–152: Catches any exception and re-raises as a 500 HTTP error with the
  exception message as the detail.


Lines 155–157: Main Entry Point
---------------------------------
- Runs only when the file is executed directly (not imported).
- Prints a startup message and starts the uvicorn server on localhost port 8000.
